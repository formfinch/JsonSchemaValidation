# JsonSchemaValidation

JsonSchemaValidation is a .NET class library for validating JSON documents against JSON Schemas without reliance on third-party libraries, built on top of `System.Text.Json`.

## Features

- Validate JSON documents against JSON Schema Draft 2020-12.
- [List other features and capabilities of your library.]

## Installation

Use the package manager [NuGet](https://www.nuget.org/) to install JsonSchemaValidation.